[pytest]
; Neden: tests/ kökündeki eski script-tarzı test dosyaları (print tabanlı,
; portal bağımlı) toplanmasın; varsayılan koşu yalnızca smoke paketini kapsar.
testpaths = tests/smoke
python_files = test_*.py
